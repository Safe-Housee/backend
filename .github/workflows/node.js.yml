name: Run tests

on: 
  push:
  pull_request:
    branches:
      - main

jobs:  
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        uses: ./
        run: npm install

      - name: where
        uses: ./
        run: ls -l

      - name: Run tests
        uses: ./
        run: npm t
