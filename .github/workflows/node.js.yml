name: Run tests

on: 
  push:
  pull_request:
    branches:
      - main

jobs:  
  test:
    runs-on: ubuntu-latest
    uses: ./
    steps:
      - name: Install dependencies
        run: npm install

      - name: where
        run: ls -l

      - name: Run tests 
        run: npm t
